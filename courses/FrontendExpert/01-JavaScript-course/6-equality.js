
/* console.log(5 == '5'); // true value only
console.log(5 === '5'); // false type AND value

console.log(NaN == NaN); // false
console.log(NaN === NaN); // false

console.log('abc' == NaN); // true

console.log(null == undefined, null, undefined); // true
console.log(null === undefined); // false */

console.log({} == {})   // false


// Which one should we use?

// loose equality (==) is more common than strict equality (===)

// strict equality (===) is more common than loose equality (==)

// :-)

